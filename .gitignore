node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
server/data/

# ASC API keys - never commit
mobile/credentials/

# Local env (never commit)
**/.env.local
.env.local

# Broadcast push secret — never commit
broadcast-secret.local
.broadcast-secret
