node_modules/
build/
dist/
coverage/
.DS_Store
*.log
__pycache__/
*.py[cod]
.env*
!.env.example
server/data/

# ASC API keys - never commit
mobile/credentials/
mobile/certs/
mobile/supabase/.temp/
mobile/credentials.json
mobile/*.p8
mobile/*.p12
mobile/*.mobileprovision
mobile/*.cer
mobile/*.certSigningRequest

# App Store metadata pull includes private App Review credentials
mobile/store.config.json
mobile/store/

# Local env (never commit)
**/.env.local
.env.local

# Broadcast push secret — never commit
broadcast-secret.local
.broadcast-secret
